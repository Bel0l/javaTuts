package Inheritance.MultiLevel;

public class ECar extends Car {
  
    int batteries;
    String distance;

        void main (){
            System.out.println(" Brand: " + brandName + " MaxSpeed: " + maxSpeed + " model: " + model + " Type: " + type + " Number of Batteries: " + batteries + " distance travel on batteries: " + distance);
        }
}
